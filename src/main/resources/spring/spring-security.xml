<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/security
       http://www.springframework.org/schema/security/spring-security.xsd">

    <security:http pattern="/api/v1/**" use-expressions="true" create-session="stateless">
        <security:http-basic/>
        <security:intercept-url pattern="/api/v1/admin/**" access="hasRole('ROLE_ADMIN')"/>
        <security:intercept-url pattern="/api/v1/profile" access="isAuthenticated()"/>
        <security:intercept-url pattern="/api/v1/restaurants" access="isAuthenticated()"/>
        <security:intercept-url pattern="/api/v1/menus" access="isAuthenticated()"/>
        <security:csrf disabled="true"/>
    </security:http>

    <bean class="org.alesapps.votingsystem.util.PasswordUtil" id="passwordEncoder" factory-method="getPasswordEncoder"/>

    <security:authentication-manager>
        <security:authentication-provider user-service-ref="userService">
            <security:password-encoder ref="passwordEncoder"/>
        </security:authentication-provider>
    </security:authentication-manager>

</beans>